# Before ghcr.io/getindiekit/indiekit is published, or to test a local build of it:
#   docker build -t indiekit:local /path/to/getindiekit/indiekit
#   docker compose -f compose.yml -f compose.local.yml up -d
services:
  indiekit:
    image: indiekit:local
